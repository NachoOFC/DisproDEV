{
  "version": 2,
  "buildCommand": "composer install --no-interaction --prefer-dist --optimize-autoloader && npm install && npm run prod",
  "outputDirectory": "public",
  "routes": [
    {
      "src": "^/(.*)$",
      "dest": "/index.php"
    }
  ]
}

